log_level: info
outputs:
  stdout:
    enabled: true

      #bucket:
      #  enabled: true
      #  url: "file:///tmp/bucket"
      #  format: "parquet"
      #  flush_seconds: 5
      #  path: "{{ .Filename }}.{{ .Format }}"
